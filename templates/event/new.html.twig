{% extends 'base.html.twig' %}

{% block title %}Réserver{% endblock %}

{% block body %}
    <h1 class="h3">Réserver</h1>

    {{ include('event/_form.html.twig') }}
    <div>
        <a class="btn btn-primary my-3 px-5 rounded-pill" href="{{ path('app_event_index') }}">Retour à la liste</a>
    </div>
    
{% endblock %}
