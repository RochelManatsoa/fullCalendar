{% extends 'base.html.twig' %}

{% block title %}Modifier une place{% endblock %}

{% block body %}
    <h1 class="h3">Modifier une place</h1>

    {{ include('place/_form.html.twig', {'button_label': 'Update'}) }}
    <div class="d-flex justify-content-end w-100">
        <a class="btn btn-primary px-5 rounded-pill" href="{{ path('app_place_index') }}">Retour à la liste</a>
        {{ include('place/_delete_form.html.twig') }}
    </div>
{% endblock %}
